export function substactNumbers(a,b) {
    return a-b;
} 

export function addNumbersInArray(elements) {
    element = 0;
      for (let i=0; i < elements.length; i++) {
          element = element + elements[i];
      }
      return element;
  }
